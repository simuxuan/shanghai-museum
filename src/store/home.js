// 引入api
// import {} from "@/api"

const state = {}

const actions = {
    async test({commit}) {

    }
}

const mutations = {
    TEST(state,value) {

    }
}

const getters = {}

export default ({
    namespaced:true,
    state,
    mutations,
    actions,
    getters
})